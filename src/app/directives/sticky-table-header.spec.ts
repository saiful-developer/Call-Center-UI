import { StickyTableHeader } from './sticky-table-header';

describe('StickyTableHeader', () => {
  it('should create an instance', () => {
    const directive = new StickyTableHeader();
    expect(directive).toBeTruthy();
  });
});
